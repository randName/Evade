using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class winConditions : MonoBehaviour {
    private bool alive; //check if the player is alive.
    private bool inArena; //check if the player is out of the arena
    private bool hasCollided; //check if the player had collided with obstacles. 
    

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
